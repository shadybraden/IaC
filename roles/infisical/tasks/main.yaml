---
- name: Setup folders for infisical
  block:
    - name: Set directory ownership recursively to uid/gid 1000
      become: true
      ansible.builtin.file:
        path: "/data/config_storage/infisical"
        owner: 1000
        group: 1000
        recurse: true
        state: directory
