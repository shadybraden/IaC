---
- name: Setup folders for samba
  block:
    - name: Set directory ownership recursively to uid/gid 1000
      become: true
      ansible.builtin.file:
        path: "/data/dont_backup/samba"
        owner: 1000
        group: 1000
        recurse: true
        state: directory
