---
- name: Setup folders for memtly
  block:
    - name: Set directory ownership recursively to uid/gid 1000
      become: true
      ansible.builtin.file:
        path: "/data/config_storage/memtly"
        owner: 1000
        group: 1000
        recurse: true
        state: directory
