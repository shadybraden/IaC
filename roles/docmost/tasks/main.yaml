---
- name: Setup folders for docmost
  block:
    - name: Set directory ownership recursively to uid/gid 1000
      become: true
      ansible.builtin.file:
        path: "/data/config_storage/docmost"
        owner: 1000
        group: 1000
        recurse: true
        state: directory
