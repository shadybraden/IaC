name: Use buildah to make an image

on:
  #push:
    #paths:
      #- '.forgejo/workflows/dockerfile_build.yaml'
      #- 'containers/Dockerfile.tools_container'
      #- 'containers/Dockerfile.komodo-core'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish - Leave blank to auto-increment.'
        required: false
        type: string
        default: ''
      Dockerfile:
        description: 'Which Dockerfile'
        required: true
        type: choice
        options:
          - tools_container
          - komodo-core
          - periphery
        default: 'tools_container'

jobs:
  Build-Dockerfile:
    runs-on: tools_container

    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Determine version
        id: version
        run: |
          INPUT_VERSION="${{ github.event.inputs.version }}"

          if [ -n "$INPUT_VERSION" ]; then
            echo "Using manually specified version: $INPUT_VERSION"
            echo "version=$INPUT_VERSION" >> $GITHUB_OUTPUT
          else
            echo "Fetching latest tag from registry..."

            # Fetch tags from Forgejo container registry API
            TAGS=$(curl -s \
              "https://git.holmlab.org/api/v1/packages/shady?type=container&limit=50" \
              | jq -r '.[] | select(.name == "${{ github.event.inputs.Dockerfile }}") | .version')

            if [ -z "$TAGS" ]; then
              echo "No existing tags found, starting at 0.0.1"
              NEW_VERSION="0.0.1"
            else
              # Sort tags as semver, pick the highest
              LATEST=$(echo "$TAGS" \
                | grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' \
                | sort -t. -k1,1n -k2,2n -k3,3n \
                | tail -n1)

              echo "Latest version found: $LATEST"

              # Bump patch
              MAJOR=$(echo "$LATEST" | cut -d. -f1)
              MINOR=$(echo "$LATEST" | cut -d. -f2)
              PATCH=$(echo "$LATEST" | cut -d. -f3)
              NEW_VERSION="$MAJOR.$MINOR.$((PATCH + 1))"
            fi

            echo "New version: $NEW_VERSION"
            echo "version=$NEW_VERSION" >> $GITHUB_OUTPUT
          fi

      - name: Build image
        run: |
          podman --version
          podman --storage-driver vfs build -f containers/Dockerfile.${{ github.event.inputs.Dockerfile }} -t ${{ github.event.inputs.Dockerfile }}:${{ steps.version.outputs.version }} .

      - name: Push image version
        run: |
          buildah --storage-driver vfs push \
            --creds "${{ secrets.REGISTRY_USERNAME }}:${{ secrets.REGISTRY_PASSWORD }}" \
            ${{ github.event.inputs.Dockerfile }}:${{ steps.version.outputs.version }} \
            docker://git.holmlab.org/shady/${{ github.event.inputs.Dockerfile }}:${{ steps.version.outputs.version }}

      - name: Push image :latest
        run: |
          buildah --storage-driver vfs push \
            --creds "${{ secrets.REGISTRY_USERNAME }}:${{ secrets.REGISTRY_PASSWORD }}" \
            ${{ github.event.inputs.Dockerfile }}:${{ steps.version.outputs.version }} \
            docker://git.holmlab.org/shady/${{ github.event.inputs.Dockerfile }}:latest

      - name: Notify failure
        if: failure()
        run: |
          curl -X POST https://ntfy.holmlab.org/${{ secrets.NTFY_TOPIC }} \
            -H "Title: ‼️ Actions failed" \
            -H "Priority: low" \
            -d "Workflow '${{ github.workflow }}' failed"
