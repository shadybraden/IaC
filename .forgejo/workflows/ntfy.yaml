name: Send ntfy notification

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: tools_container

    steps:
      - name: Send ntfy notification
        run: |
          curl -d "hello from actions" https://ntfy.holmlab.org/${{ secrets.NTFY_TOPIC }}
