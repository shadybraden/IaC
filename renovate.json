{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:base"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update Docker image versions in YAML label fields",
      "fileMatch": [
        "\\.ya?ml$"
      ],
      "matchStrings": [
        "docker://(?<depName>[^:]+):(?<currentValue>[\\d][^\\s\"']+)"
      ],
      "datasourceTemplate": "docker",
      "versioningTemplate": "semver"
    }
  ]
}